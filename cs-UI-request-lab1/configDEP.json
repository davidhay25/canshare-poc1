{
  "description": "The configuration for the POC reference implementation",
  "canShare": {
    "fhirServer" : {
      "url" : "http://localhost:8080/fhir",
      "description" : "The base url of the FHIR server"
    },
    "requestEndPoint" : {
      "url": "http://localhost:9300/$acceptRequest",
      "description" : "The endpoint where the requester sends the request bundle (SR, SQ etc"
    }
  },
  "lab": {
    "srEndpoint" : {
      "url": "localhost:9100/lab/ServiceRequest",
      "description": "The endpoint to send SR notifications to the lab"
    },
    "reportEndpoint" : {
      "url": "http://localhost:1880/acceptReport",
      "description" : "The endpoint where the lab sends the report bundle"
    }
  }
}